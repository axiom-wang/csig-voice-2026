# CSIG 正发声 · AI 的加速度 —— 正文页运动规范

适用范围：正文七幕滚动画卷（挂载于 `site/index.html` 的 `#storyMount`），**不含封面首页**。封面揭开转场完成后，本规范接管一切运动。

与 `叙事分镜.md` 的关系：分镜定义"演什么"，本规范定义"怎么动"。两者冲突时，叙事内容以分镜为准，运动方式以本规范为准。

## 核心模型：一张世界，一个镜头

正文不是多个 section 的切换，而是**一张巨大的二维手稿世界**。用户滚动时，不是页面内容向上移动，而是 scroll progress 控制一个虚拟 camera 在手稿世界中移动。

- **世界层（World）**：单一二维坐标空间，七幕所有事件、纸张、插画、线条、年份旁注共享同一坐标系，相对位置与尺度永久固定。世界只有一张，不做按幕拆分。
- **镜头（Camera）**：由滚动进度驱动的虚拟取景框，状态为 `(x, y, scale)`。camera 可以横向移动、纵向移动、轻微 zoom in / zoom out，全程不解绑、不瞬移跳切（跨时间线转场也靠连续移动与对象变形完成）。
- **屏幕层（Screen）**：历史顶部时间轴、幕级主句、活动信息与主按钮，固定于视口安全区，不随世界平移缩放（沿用分镜分层约定）。
- **禁止**：把每个事件做成独立 section 后 fade in / fade out；整屏黑场、白场、交叉淡化作为默认转场。

## 1. Scroll controls time

用户滚动 = 时间推进；用户停止滚动 = 大多数动画停止。

- 全篇主时间轴是一条与滚动进度绑定的 scrub 时间轴：`scrollProgress ∈ [0, 1]` 映射到画卷全局时间 `t`。
- Primary motion（见第 8 节）全部挂在主时间轴上，滚动停止即冻结在当前状态；不允许滚动停止后仍自行播放的叙事动画。
- 仅 Ambient motion 允许脱离滚动做微弱的自循环（见第 8 节上限）。
- 向上滚动 = 时间倒流：镜头、对象状态、绘制进度、视觉主题**同步反向还原**，不做"回滚时直接跳回最终态"的取巧处理。

## 2. Camera moves through one world

- 镜头路径用关键帧描述：`(progress, x, y, scale)`，关键帧之间连续插值。路径设计必须与总构图共同完成（沿用分镜"双端镜头路径"约定）。
- 移动方式：平移为主，zoom 为辅。zoom 范围收敛在 `0.6 – 1.6`（相对基准取景），推近用于聚焦单一手稿细节，拉远用于揭示密度与全景（如 3D 事件群、第七幕圆桌）。
- 镜头转向时沿弧线过渡，不做生硬的直角折返；平移与缩放可叠加，但同一时刻只突出一个运动目的（在赶路 / 在细看 / 在拉远揭示）。
- 移动端是同一世界的**较窄镜头裁切**：共享世界坐标与素材位置，仅重设镜头位置、缩放与停留区间，不重新排列世界元素，不把桌面整幅缩小塞屏。

## 3. Objects transform instead of disappear

场景切换优先采用对象变形，而不是旧元素消失、新元素出现。

- 分镜已锁定的可追踪变形链，必须连续完成，中间不断裂：
  - 问号尾笔 → 研究主线（第一幕 → 第二幕）
  - 神经元/规则线条 → 图片网格 → 围棋网格 → 棋子变 Token → 连接汇入输入框（第二幕 → 第四幕）
  - 事件纸片与论文页边 → 当前工作稿轮廓（第三幕 → 第四幕）
  - 问题纸 → 圆桌中央（第六幕 → 第七幕）
- 变形手段按优先级：路径 morph（SVG path 插值）> 形变（transform: translate/scale/rotate）> 连线关系重排 > 透明度辅助过渡。透明度只允许作为变形的辅助，不允许单独承担"换场"。
- 已出现的事件与物件**持续留在世界内**（尤其 3D 事件群），后续内容在其周围累积；确需退场时优先移出镜头视野，而非原地消失。
- 材质切换（历史羊皮纸 ⇄ 当前打印纸）跟随对象与镜头同步渐变，由主题参数（墨色、线宽、透明度、笔触不规则度）插值完成，不整屏换肤。

## 4. Drawing animations should feel physical

线条必须像正在被笔画出来。

- 所有线条生长使用 SVG path 描线（`stroke-dasharray` / `stroke-dashoffset`，配合 `pathLength="1"` 归一化），不用宽度模拟、不用裁剪假生长。
- 描线速度曲线模拟真实运笔：**起笔稍慢、行笔均匀、收笔有顿挫**；长线条允许分段接力（一条画完带出下一条），不做全程匀速的机械描边。
- 擦除与重写（第二幕"被划掉的研究方案"）同样要有物理感：划除笔触可见，重写在他处起笔，保留尝试痕迹。
- 批注、圈选、删除线的笔迹允许轻微不规则（抖动、压感式线宽变化），但不规则程度是主题参数，不随机到影响可读性。

## 5. Scroll velocity affects drawing velocity

- 绘制速度与滚动速度联动：慢滚时逐笔清晰可见；快速滚动时绘制同步加快。
- **加速不跳帧**：无论滚动多快，关键状态（见第 7 节清单）必须完整到达并可在回滚时还原；快滚只能压缩过程时长，不能跳过状态。实现上以滚动进度（而非时间）作为绘制的唯一驱动量，天然满足此条。
- 反向滚动时，绘制按原路径反向擦回；回滚到节点之前，节点文字与画面恢复未绘制状态。

## 6. Density increases over time

- 世界的手稿密度沿叙事单调递增：第二幕稀疏大留白 → 第三幕网格、落子、连接逐渐加密 → 3D 生成式 AI 区域在同一世界区域内快速累积、留白收紧 → 第四幕后以当前工作稿的批注与协作线延续密度。
- 密度增长靠**新增元素持续入场并保留**实现，不靠元素变大或复制堆叠。
- 历史顶部时间轴的刻度间距（1956 ⟶ 1997 ⟶ 2012 ⟶ … ⟶ 2022—26 年份簇）与下方世界密度呼应，作为加速度的常驻参照。
- 密度增长受可读性约束：任何镜头的文字不得因密度被挤压至不可读（移动端按分镜的错列与保留区间处理）。

## 7. Use pauses for important moments

- 到达关键状态时，camera 可以**短暂减速**——通过镜头路径在关键帧附近放缓插值速率（时间轴上设置速率平台）实现"自然停留感"。
- **不强制锁死滚动**：不使用 scroll-jacking、不劫持滚轮、不设置必须等待播放完才能继续的关卡；用户在"停顿区"内依然可以随时滚走或滚回。
- 关键状态（减速区）清单：1956 会议与命名、AlexNet 网格成形、AlphaGo 落子、Transformer 连接成形、3D 事件群完整累积、输入框收拢、Demo 完成、三张工作桌汇合、第七幕活动信息完整显现。

## 8. Motion hierarchy

不是所有东西都同时动。任何时刻以一层为主导，其余退后。

| 层级 | 内容 | 驱动方式 | 约束 |
|---|---|---|---|
| **Primary** | camera 移动、核心对象 morph、关键线条生长（研究主线、变形链） | 严格绑定滚动进度（scrub） | 滚动停则停；反向滚动完整还原；全程只允许一处主导性运动 |
| **Secondary** | 批注出现、参数修改、照片/纸片放置、便签挤入、待办勾选 | 绑定滚动进度，在所属 Primary 段落内错峰进入 | 时序错开，不与 Primary 抢视觉焦点；同样支持反向还原 |
| **Ambient** | 纸张轻微纹理、光标、偶尔微小的笔迹变化、封面遗留的暖光呼吸 | 可脱离滚动做自循环 | 必须极弱：透明度变化幅度 ≤ 0.1，位移 ≤ 2px，频率低、可随时整体关闭；**不能让网页像动态壁纸** |

- 同一画面同时处于运动状态的元素数应有上限：一个 Primary + 至多两个 Secondary + Ambient 背景层。
- 新运动入场前，先让上一个 Primary 收束，避免多个变形链并行。

## 9. Technical principle

按优先级选型，能用简单手段达到效果就不用复杂手段：

1. **SVG path animation**：一切线条生长、描线、路径 morph 的首选。
2. **CSS transform / opacity**：对象位移、缩放、旋转与辅助透明度；所有高频动画属性仅限 `transform` 与 `opacity`，避免触发 layout 的属性动画。
3. **GSAP ScrollTrigger（或 Motion 等价能力）**：主时间轴 scrub、镜头路径插值、分层错峰的统一实现工具。
4. **Canvas**：仅用于非常密集的网络（如 Transformer 之后的 Token 连接网在密度超过 SVG 可维护上限时）；Canvas 内容仍需遵守反向还原与滚动绑定。

禁止默认使用：3D 场景与 WebGL 物理仿真、粒子系统、复杂 shader。确有必要时须单独论证，且提供等价降级。
